---
// Auto-generated by tools/generate.mjs — do not edit directly.
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import site from '../../data/site.json';
---

<Layout title={"New Patient Special - AnaBella Family and Cosmetic Dentistry"} description={"Take advantage of our new patient special offer for the best dental care in Bellevue WA at AnaBella Family and Cosmetic Dentistry today."} canonical={"https://www.anabelladentistry.com/special/"} ogImage={""}>
<script type="application/ld+json" set:html={"{\"@context\":\"https:\\/\\/schema.org\",\"@graph\":[{\"@type\":\"WebPage\",\"@id\":\"https:\\/\\/www.anabelladentistry.com\\/special\\/\",\"url\":\"https:\\/\\/www.anabelladentistry.com\\/special\\/\",\"name\":\"New Patient Special - AnaBella Family and Cosmetic Dentistry\",\"isPartOf\":{\"@id\":\"https:\\/\\/www.anabelladentistry.com\\/#website\"},\"datePublished\":\"2026-02-09T16:29:52+00:00\",\"dateModified\":\"2026-02-09T19:51:44+00:00\",\"description\":\"Take advantage of our new patient special offer for the best dental care in Bellevue WA at AnaBella Family and Cosmetic Dentistry today.\",\"breadcrumb\":{\"@id\":\"https:\\/\\/www.anabelladentistry.com\\/special\\/#breadcrumb\"},\"inLanguage\":\"en-US\",\"potentialAction\":[{\"@type\":\"ReadAction\",\"target\":[\"https:\\/\\/www.anabelladentistry.com\\/special\\/\"]}]},{\"@type\":\"BreadcrumbList\",\"@id\":\"https:\\/\\/www.anabelladentistry.com\\/special\\/#breadcrumb\",\"itemListElement\":[{\"@type\":\"ListItem\",\"position\":1,\"name\":\"Home\",\"item\":\"https:\\/\\/www.anabelladentistry.com\\/\"},{\"@type\":\"ListItem\",\"position\":2,\"name\":\"Special Offer\"}]},{\"@type\":\"WebSite\",\"@id\":\"https:\\/\\/www.anabelladentistry.com\\/#website\",\"url\":\"https:\\/\\/www.anabelladentistry.com\\/\",\"name\":\"AnaBella Family and Cosmetic Dentistry\",\"description\":\"Dentist in Bellevue WA\",\"potentialAction\":[{\"@type\":\"SearchAction\",\"target\":{\"@type\":\"EntryPoint\",\"urlTemplate\":\"https:\\/\\/www.anabelladentistry.com\\/?s={search_term_string}\"},\"query-input\":{\"@type\":\"PropertyValueSpecification\",\"valueRequired\":true,\"valueName\":\"search_term_string\"}}],\"inLanguage\":\"en-US\"}]}"}></script>

  <Header />

  <!-- ── Hero ── -->
  <section class="sp-hero">
    <div class="container sp-hero-inner">
      <p class="sp-prefix">NEW PATIENT SPECIAL OFFER</p>
      <h1>Complimentary Consultation For Clear Aligners, Cosmetic Dentistry, Implants, TMJ Pain, Or Sleep Apnea</h1>
    </div>
  </section>
  <!-- downward triangle -->
  <div class="sp-arrow-down" aria-hidden="true"></div>

  <!-- ── Content + Form ── -->
  <section class="sp-body">
    <div class="container sp-grid">

      <div class="sp-text">
        <p>If you're seeking a natural approach to restoring your oral health and improving your quality of life, now is the perfect time to take advantage of our <strong>exclusive offer!</strong></p>
        <p>Our skilled dentists adopt a "whole mouth" philosophy, working collaboratively with you to understand your specific dental needs. Together, we'll create a personalized care plan that supports your journey toward optimal oral health and a confident smile.</p>
        <p>We also offer a unique, comprehensive and custom-to-you dental program that will help you achieve your smile goals. Don't just take our word for it—explore our <a href="/testimonials/" class="sp-link">patient testimonials</a> to see the impact we've made in others' lives.</p>
        <p><strong>Book Your Appointment Today!</strong> Discover the Unique Benefits of Our Dental Care!</p>
      </div>

      <div class="sp-form-col">
        <p>Simply fill out the form below, and we'll be in touch soon to get you started.</p>

        <a href={site.zocDocUrl} class="sp-btn sp-btn-schedule" target="_blank" rel="noopener noreferrer">
          Or Schedule Online Now
        </a>

        <form class="sp-form" id="special-form" novalidate>
          <div class="sp-field">
            <input type="text" id="name" name="name" placeholder="Full Name*" required autocomplete="name" />
          </div>
          <div class="sp-field">
            <input type="email" id="email" name="email" placeholder="Your Email*" required autocomplete="email" />
          </div>
          <div class="sp-field">
            <input type="tel" id="phone" name="phone" placeholder="Your Cell Phone*" autocomplete="tel" />
          </div>
          <div class="sp-field">
            <select id="service" name="service">
              <option value="" disabled selected>Choose a Service</option>
              <option value="Clear Aligners">Clear Aligners</option>
              <option value="Cosmetic Dentistry">Cosmetic Dentistry</option>
              <option value="Dental Implants">Dental Implants</option>
              <option value="TMJ Pain">TMJ Pain</option>
              <option value="Sleep Apnea">Sleep Apnea</option>
              <option value="Other">Other</option>
            </select>
          </div>
          <div class="sp-field">
            <textarea id="message" name="message" placeholder="How Can We Help?"></textarea>
          </div>
          <button type="submit" class="sp-btn sp-btn-submit" id="submit-btn">SUBMIT</button>
          <p class="sp-status" id="form-status" aria-live="polite"></p>
        </form>
      </div>

    </div>
  </section>

  <!-- upward triangle -->
  <div class="sp-arrow-up" aria-hidden="true"></div>
  <!-- ── Bottom CTA ── -->
  <section class="sp-cta">
    <div class="container sp-cta-inner">
      <p class="sp-cta-pre">Don't Wait Another Day</p>
      <h2>SCHEDULE YOUR APPOINTMENT TODAY!</h2>
    </div>
  </section>

  <Footer />
</Layout>

<script>
  const form = document.getElementById('special-form') as HTMLFormElement;
  const submitBtn = document.getElementById('submit-btn') as HTMLButtonElement;
  const status = document.getElementById('form-status') as HTMLParagraphElement;

  form.addEventListener('submit', async (e) => {
    e.preventDefault();
    submitBtn.disabled = true;
    submitBtn.textContent = 'Sending…';
    status.textContent = '';
    status.className = 'sp-status';

    try {
      const res = await fetch('/api/special', {
        method: 'POST',
        body: new FormData(form),
      });
      const json = await res.json();

      if (res.ok) {
        form.reset();
        status.textContent = 'Thank you! We will be in touch soon to get you started.';
        status.className = 'sp-status success';
      } else {
        status.textContent = json.error ?? 'Something went wrong. Please try again.';
        status.className = 'sp-status error';
      }
    } catch {
      status.textContent = 'Network error. Please check your connection and try again.';
      status.className = 'sp-status error';
    } finally {
      submitBtn.disabled = false;
      submitBtn.textContent = 'SUBMIT';
    }
  });
</script>

<style>
  /* ── Shared token ── */
  :root {
    --sp-bg: #a1c8aa;
    --sp-btn: #83b78f;
    --sp-text-on-bg: #f7f7f7;
  }

  /* ── Hero ── */
  .sp-hero {
    background: var(--sp-bg);
    padding: 3.5rem 0 2.5rem;
    text-align: center;
  }
  .sp-prefix {
    font-family: var(--font-heading);
    font-size: clamp(1.1rem, 3vw, 1.6rem);
    font-weight: 700;
    letter-spacing: 0.05em;
    text-transform: uppercase;
    color: var(--sp-text-on-bg);
    margin-bottom: 0.75rem;
    display: block;
  }
  .sp-hero h1 {
    font-family: var(--font-heading);
    font-size: clamp(0.95rem, 2vw, 1.15rem);
    font-weight: 400;
    color: var(--sp-text-on-bg);
    max-width: 680px;
    margin: 0 auto;
    line-height: 1.5;
    text-transform: capitalize;
  }

  /* ── Triangle arrows ── */
  .sp-arrow-down {
    width: 0;
    height: 0;
    border-left: 55px solid transparent;
    border-right: 55px solid transparent;
    border-top: 38px solid var(--sp-bg);
    margin: 0 auto;
  }
  .sp-arrow-up {
    width: 0;
    height: 0;
    border-left: 55px solid transparent;
    border-right: 55px solid transparent;
    border-bottom: 38px solid var(--sp-bg);
    margin: 0 auto;
  }

  /* ── Body ── */
  .sp-body {
    padding: 3rem 0;
    background: #fff;
  }
  .sp-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    align-items: start;
  }
  .sp-text p {
    margin-bottom: 1rem;
    line-height: 1.75;
    color: var(--color-text-main);
  }
  .sp-link {
    color: var(--color-accent-hover);
    text-decoration: underline;
    text-underline-offset: 2px;
  }

  /* ── Form column ── */
  .sp-form-col > p {
    margin-bottom: 1rem;
    line-height: 1.75;
    color: var(--color-text-main);
  }

  /* ── Buttons ── */
  .sp-btn {
    display: block;
    width: 100%;
    text-align: center;
    background: var(--sp-btn);
    color: #fff;
    font-family: var(--font-heading);
    font-weight: 700;
    font-size: 0.9rem;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    padding: 0.85rem 1rem;
    border-radius: 3px;
    border: none;
    cursor: pointer;
    transition: background 0.2s;
    box-sizing: border-box;
    text-decoration: none;
  }
  .sp-btn:hover { background: #6fa87b; }
  .sp-btn-schedule { margin-bottom: 1.25rem; }
  .sp-btn-submit { margin-top: 0.5rem; }

  /* ── Form fields ── */
  .sp-form { display: flex; flex-direction: column; gap: 0; }
  .sp-field { margin-bottom: 0.6rem; }
  .sp-field input,
  .sp-field select,
  .sp-field textarea {
    width: 100%;
    padding: 0.7rem 1rem;
    background: #fff;
    border: 1px solid #ccc;
    border-radius: 3px;
    font-family: inherit;
    font-size: 0.9rem;
    color: var(--color-text-main);
    box-sizing: border-box;
    transition: border-color 0.2s;
  }
  .sp-field input:focus,
  .sp-field select:focus,
  .sp-field textarea:focus {
    outline: none;
    border-color: var(--sp-btn);
  }
  .sp-field select { appearance: auto; }
  .sp-field textarea { resize: vertical; min-height: 80px; }
  .sp-field input::placeholder,
  .sp-field textarea::placeholder { color: #999; }

  /* ── Status ── */
  .sp-status { margin-top: 0.75rem; font-weight: 600; font-size: 0.9rem; }
  .sp-status.success { color: #2d7a3a; }
  .sp-status.error { color: #c0392b; }

  /* ── Bottom CTA ── */
  .sp-cta {
    background: var(--sp-bg);
    padding: 2.5rem 0;
    text-align: center;
  }
  .sp-cta-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.4rem;
  }
  .sp-cta-pre {
    font-family: var(--font-heading);
    font-size: 0.85rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--sp-text-on-bg);
    opacity: 0.9;
  }
  .sp-cta h2 {
    font-family: var(--font-heading);
    font-size: clamp(1.2rem, 3vw, 1.9rem);
    font-weight: 700;
    color: var(--sp-text-on-bg);
    text-transform: uppercase;
    letter-spacing: 0.03em;
  }

  /* ── Responsive ── */
  @media (max-width: 768px) {
    .sp-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }
    .sp-arrow-down,
    .sp-arrow-up {
      border-left-width: 40px;
      border-right-width: 40px;
      border-top-width: 28px;
      border-bottom-width: 28px;
    }
  }
</style>
